import React from 'react';
import ReactDOM from 'react-dom/client';

// You might want to import your main App component and global CSS here later
// import App from './App.tsx';
// import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    {/* Replace this with your main App component once created */}
    <div>Loading Application...</div>
  </React.StrictMode>,
);